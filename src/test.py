# распечатка нескольких строк с сохранением каждого документа
from src.main import *

# Проверяем в таблице поле "Печать"

# Определяем, в каком по счёту столбце находится заголовок с именем flag_column_name




get_column_id('Имя')

flag_column_id
    print(header.id)
header.name
    table.tableColumns.name


for row in ws[table.ref]:
    for cell in row:
        if cell.column ==

        print (cell.column)





        print(cell.value)


